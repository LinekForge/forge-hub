# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "forge-hub"

[setup]
script = '''
set -euo pipefail

export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"
cd "$CODEX_WORKTREE_PATH"

for dir in hub-server hub-client forge-engine hub-dashboard; do
  (cd "$dir" && bun install --frozen-lockfile --ignore-scripts)
done
'''

[[actions]]
name = "验证"
icon = "test"
command = '''
set -euo pipefail
export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"

cd "$CODEX_WORKTREE_PATH"

(cd hub-server && bunx tsc --noEmit && bun test)
(cd hub-client && bunx tsc --noEmit)
(cd forge-engine && bunx tsc --noEmit && bun test)
bun test forge-cli
(cd hub-dashboard && bun run build)
bun hub-test-harness/harness.ts
'''
